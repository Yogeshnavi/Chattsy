# Chat App

## Structure
Frontend/ → Next.js app  
server/ → Node.js + Express + Socket.IO

## How to run
1. server: `cd server && npm install && npm run dev`
2. Frontend: `cd Frontend/frontend && npm install && npm run dev`
